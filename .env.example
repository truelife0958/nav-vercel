# JWT 密钥 (必填，用于生成认证 token)
JWT_SECRET=your-super-secret-key-change-this-in-production

# Node 环境
NODE_ENV=production

# 数据库路径 (Vercel 环境自动使用 /tmp)
DB_PATH=/tmp/nav.db

# CORS 允许的域名 (多个域名用逗号分隔)
ALLOWED_ORIGINS=*

# 默认管理员账号 (可选，用于初始化)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=123456
